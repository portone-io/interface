syntax = "proto3";
package authenticate;

option go_package = "github.com/iamport/interface/build/go/authenticate";

message Token {
    string access_token = 1;
    int32 now = 2;
    int32 expired_at = 3;
}

message TokenRequest {
    string imp_key = 1;
    string imp_secret = 2;
}